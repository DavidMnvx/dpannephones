services:
###> doctrine/doctrine-bundle ###
  database:
    ports:
      - "3306:3306"
###< doctrine/doctrine-bundle ###

###> symfony/mailer ###
# Mailpit désactivé — emails envoyés via Brevo (voir MAILER_DSN dans .env.local)
###< symfony/mailer ###
